import React from "react";
import {ReactComponent as Icon} from '../../../assets/icons/help.svg';
import Wrapper from "../_components/Wrapper";

const Help = (props) => (
    <Wrapper {...props}>
        <Icon/>
    </Wrapper>
);

export default Help;
